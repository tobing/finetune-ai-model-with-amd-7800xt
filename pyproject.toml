[project]
name = "finetune"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tobing", email = "lumban.tobing.m@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "datasets>=5.0.1",
    "openai>=3.1.0",
    "psutil>=7.2.2",
    "transformers>=5.15.0",
]

[project.scripts]
finetune = "finetune.train:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
